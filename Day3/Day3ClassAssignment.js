 const browserVersion = "chrome"
 function getBrowserVersion(){
    // var browserVersion
    if(browserVersion=="chrome"){
        // var browserVersion = "chrome" // Undefined
        let browserVersion ="chrome" // Chrome
    }
    console.log("version number inside the function",browserVersion);
    
    
 }
//  console.log("version number outside the function",browserVersion);

 getBrowserVersion()


